# Известные проблемы и ошибки (Issue Tracker)

## Текущие вызовы (Technical Challenges)

### 1. Ошибка Prisma 7 (P1012): `url` в схеме больше не поддерживается
**Описание**: В Prisma 7 произошли радикальные изменения в работе с источниками данных. Прямое указание `url = env("DATABASE_URL")` в `schema.prisma` выдает ошибку валидации.
**Статус**: 🛠 В процессе исправления.
**Решение**: Необходимо перенести конфигурацию в `prisma.config.ts` и использовать Driver Adapter. Все `PRISMA_CLIENT` теперь инициализируются через адаптер `pg`.

### 2. Поддержка PostGIS в Prisma
**Описание**: Prisma до сих пор не поддерживает типы геометрии (Point) «из коробки». Они определены как `Unsupported("geometry(Point, 4326)")`.
**Риск**: Прямое обновление этих полей через Prisma Client (например, `create`) может не работать стандартным образом.
**Решение**: Использовать `prisma.$queryRaw` для обновлений и расширенного поиска. В будущем возможен переход на другой ORM (например, Drizzle), если Prisma 7 не улучшит поддержку геометрии.

### 3. Загрузка фото (Photo Upload)
**Описание**: В данный момент функционал загрузки изображений для заданий отсутствует.
**Решение**: Планируется использование `sharp` для оптимизации (конвертация в WebP) и временное хранение на локальном диске Docker-контейнера перед переходом на S3/MinIO.

### 4. Синхронизация данных пользователя ✅ ИСПРАВЛЕНО (05.04.2026)
**Описание**: При первом входе через Telegram необходимо гарантировать создание записи в БД. В условиях высокого параллелизма возможны Race Conditions при проверке существования пользователя.
**Решение**: Применён атомарный `db.user.upsert({where:{telegramId},update:{...},create:{...}})` в `src/features/auth/model/actions.ts`.

### 5. Docker: Windows Path Issues
**Описание**: При разработке на Windows возможны проблемы с маппингом томов (volumes) в Docker.
**Решение**: Рекомендуется использовать WSL2 или запускать скрипты через `dev.ps1` (настроен в `package.json`).

## История исправлений (Recent Fixes)
- [x] **23.03.2026**: Исправлена ошибка авторизации: `initData` теперь валидируется строго на сервере.
- [x] **28.03.2026**: Настроен Nginx Proxy для корректной передачи заголовков `X-Forwarded-For`.
- [x] **01.04.2026**: Исправлены типы в `TaskFeed` виджете (замена `any` на строгую типизацию).
- [x] **05.04.2026 — Hotfix авторизации (Этап 0 из [06_Development_Plan.md](06_Development_Plan.md))**:
    - A1: `TelegramAuth.tsx` — добавлен `router.refresh()` перед push на `/dashboard` (устранена петля редиректов из-за RSC-кэша). Plus добавлен polling для `window.Telegram.WebApp` (до 2с) на случай медленной загрузки скрипта.
    - A2: Сгенерирован реальный `JWT_SECRET` (32 байта base64) в `.env`. `.env.example` теперь содержит инструкцию и пустое значение.
    - A3: Удалён мёртвый `src/app/providers.tsx` (нигде не импортировался).
    - A4: Убрана ссылка на несуществующий `/api/auth/telegram` из `src/proxy.ts`.
    - A5: Добавлена проверка TTL `auth_date` (24ч) в `validateTelegramWebAppData`.
    - A6: Функция `validateTelegramWebAppData` теперь возвращает типизированный результат с причиной отказа (`no_token | no_hash | bad_signature | expired`) — убран `any`-cast в `encrypt()`.
    - Бонус: `loginWithTelegram` — заменена пара `findUnique + create` на атомарный `upsert` (устраняет race condition из Known Issue #4). Ошибки теперь логируются с префиксом `[loginWithTelegram]`.

- [x] **08.04.2026 — UI/UX & Build Stability**:
    - **Infrastructure**: Исправлена критическая ошибка билда Tailwind CSS v4, связанная с вложенными `@utility`. Все кастомные утилиты перенесены на верхний уровень `globals.css`.
    - **Aesthetics**: Реализована полная синхронизация неоновых градиентов (бордер + текст) для бейджей категорий. Сетка `CategoryGrid` сделана более компактной.
    - **UX / Maps**: Внедрена интеграция с Яндекс Картами через `shared/lib/maps.ts`. Адреса в ленте и на детальной странице стали кликабельными (активными ссылками).
    - **Fix**: Решена проблема «пропадающих» аватарок Telegram на страницах отзывов и в ленте через глобальную настройку `referrerPolicy="no-referrer"` в компоненте `Avatar`.
    - **Refinement**: Страница отзывов переработана для более высокой плотности информации (компактные карточки).
